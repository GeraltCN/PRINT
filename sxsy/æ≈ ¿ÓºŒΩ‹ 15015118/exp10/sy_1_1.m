clear
                                % 输入数据
x = [1.2 1.8 2.1 2.4 2.6 3.0 3.3];
y = [4.85 5.2 5.6 6.2 6.5 7.0 7.5];

                                % 分别生成4,5,6阶的拟合曲线
for n = 4:6
  p = polyfit(x, y, n);
  x1 = 1:0.05:3;
  y1 = polyval(p, x1);
  subplot(1,3,n-3)
  plot(x, y, ' * ', x1, y1, ':')
end
